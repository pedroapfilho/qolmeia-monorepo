# Copy to .dev.vars (git-ignored) for local `wrangler dev`.
# Tests stub the model, so .dev.vars is only needed for manual chat testing.
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# HMAC secret for /assets/:id signed URLs. Generate any 32+ byte hex string:
#   openssl rand -hex 32
ASSETS_SIGNING_KEY=replace-me-with-a-random-32-byte-hex-string

# Shared secret with apps/api for the /api/internal routes.
# Must match apps/api INTERNAL_SHARED_SECRET byte for byte, and apps/api
# requires at least 32 characters. Rotating breaks org create until both
# sides are redeployed.
INTERNAL_SHARED_SECRET=replace-me-with-the-same-value-as-apps-api

# Exa web-search API key (https://exa.ai) for the webSearch skill. Optional —
# leave unset and the skill returns a clear "indisponível" error.
EXA_API_KEY=

# Firecrawl for the fetchUrl skill (reads a webpage as markdown).
# Cloud (https://api.firecrawl.dev) needs FIRECRAWL_API_KEY (fc-...). To run
# keyless, self-host Firecrawl and set FIRECRAWL_BASE_URL to your instance.
FIRECRAWL_API_KEY=
# FIRECRAWL_BASE_URL=http://localhost:3002
