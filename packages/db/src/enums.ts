/**
 * Runtime-free re-export of the generated Prisma enums. Safe to import from
 * browser bundles and from the shared contract package: it pulls in constants
 * only, never the Prisma client or an adapter.
 */
export * from "./generated/prisma/enums";
